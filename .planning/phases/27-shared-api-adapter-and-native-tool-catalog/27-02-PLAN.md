---
phase: 27-shared-api-adapter-and-native-tool-catalog
plan: 02
type: execute
wave: 2
depends_on:
  - 27-01
files_modified:
  - packages/openclaw-plugin/src/index.ts
  - packages/openclaw-plugin/src/register-tools.ts
  - packages/openclaw-plugin/test/read-tools.test.ts
  - packages/openclaw-plugin/test/plugin-bootstrap.test.ts
  - packages/openclaw-plugin/test/tool-registration.test.ts
  - packages/mcp/src/tools/habits.ts
  - packages/mcp/src/tools/today.ts
  - packages/mcp/src/tools/stats.ts
  - packages/mcp/test/tools/habits-read.test.ts
  - packages/mcp/test/tools/today-stats-read.test.ts
autonomous: true
requirements:
  - OCP-02
  - SHRD-01
  - SHRD-03
user_setup: []
must_haves:
  truths:
    - The native plugin must expose the same read intent coverage the shipped MikoshiTracker tool surface already promises.
    - Native read handlers must use the shared API runtime and the same contracts/routes as the shipped MCP handlers.
    - Read tool implementation must stay native and must not regress registration/bootstrap behavior.
  artifacts:
    - Real native handlers for `habits_list`, `habits_get_detail`, `today_get_summary`, and `stats_get_overview`
    - Plugin tests that pin request behavior, structured read payloads, and bootstrap registration
    - Shared read-operation reuse that avoids copying endpoint and summary logic into the plugin package
  key_links:
    - Do not fork the read tool names or read schemas for OpenClaw.
    - Do not bypass the shared API client or duplicate route logic in `packages/openclaw-plugin`.
    - Do not widen this plan into mutation tools or final envelope redesign.
---

<objective>
Implement the native OpenClaw read tools on top of the shared MikoshiTracker API runtime.

Purpose: replace the plugin placeholders for read-only habits/today/stats operations with direct API-backed handlers that preserve the shipped MikoshiTracker vocabulary and reuse the shared runtime seam from Plan 01.
Output: functioning native read tools for habits, today summary, and stats overview, with tests that lock endpoint parity and native bootstrap continuity.
</objective>

<execution_context>
@/Users/finn/.codex/get-shit-done/workflows/execute-plan.md
@/Users/finn/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-shared-api-adapter-and-native-tool-catalog/27-CONTEXT.md
@.planning/phases/27-shared-api-adapter-and-native-tool-catalog/27-RESEARCH.md
@.planning/phases/27-shared-api-adapter-and-native-tool-catalog/27-VALIDATION.md
@packages/mcp/src/tools/habits.ts
@packages/mcp/src/tools/today.ts
@packages/mcp/src/tools/stats.ts
@packages/openclaw-plugin/src/index.ts
@packages/openclaw-plugin/src/register-tools.ts
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement native `habits_list` and `habits_get_detail` on the shared runtime seam</name>
  <files>packages/openclaw-plugin/src/index.ts, packages/openclaw-plugin/src/register-tools.ts, packages/openclaw-plugin/test/read-tools.test.ts, packages/mcp/src/tools/habits.ts, packages/mcp/test/tools/habits-read.test.ts</files>
  <action>Follow TDD. Start with failing native plugin tests that prove `habits_list` and `habits_get_detail` call the correct MikoshiTracker API endpoints through the shared client, parse the same schemas, and return structured read data instead of `PHASE_27_PENDING`. Then implement the smallest shared read-operation reuse needed so the plugin gets real behavior without rewriting path logic or summaries in a second place.</action>
  <verify>
    <automated>pnpm typecheck && pnpm --filter @mikoshi-tracker/openclaw-plugin exec vitest run test/read-tools.test.ts && pnpm --filter @mikoshi-tracker/mcp exec vitest run test/tools/habits-read.test.ts</automated>
  </verify>
  <done>The native plugin can now read habit lists and habit detail directly from the shipped MikoshiTracker API.</done>
</task>

<task type="auto">
  <name>Task 2: Implement native `today_get_summary` and `stats_get_overview` while preserving bootstrap registration and shared read semantics</name>
  <files>packages/openclaw-plugin/src/index.ts, packages/openclaw-plugin/src/register-tools.ts, packages/openclaw-plugin/test/read-tools.test.ts, packages/openclaw-plugin/test/plugin-bootstrap.test.ts, packages/openclaw-plugin/test/tool-registration.test.ts, packages/mcp/src/tools/today.ts, packages/mcp/src/tools/stats.ts, packages/mcp/test/tools/today-stats-read.test.ts</files>
  <action>Follow TDD. Add failing tests that prove the plugin's today/stats read tools call the same endpoints and preserve the same intent coverage as the shipped MikoshiTracker read surface, while the native bootstrap still registers the full catalog through the existing contract. Implement only the minimal shared-runtime-backed handler wiring needed to make those tests pass.</action>
  <verify>
    <automated>pnpm typecheck && pnpm --filter @mikoshi-tracker/openclaw-plugin exec vitest run test/read-tools.test.ts test/plugin-bootstrap.test.ts test/tool-registration.test.ts && pnpm --filter @mikoshi-tracker/mcp exec vitest run test/tools/today-stats-read.test.ts</automated>
  </verify>
  <done>The native plugin now exposes the full read side of the MikoshiTracker habits/today/stats catalog through real API-backed handlers.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm typecheck && pnpm --filter @mikoshi-tracker/openclaw-plugin exec vitest run test/read-tools.test.ts test/plugin-bootstrap.test.ts test/tool-registration.test.ts && pnpm --filter @mikoshi-tracker/mcp exec vitest run test/tools/habits-read.test.ts test/tools/today-stats-read.test.ts` passes
- [ ] `habits_list`, `habits_get_detail`, `today_get_summary`, and `stats_get_overview` use the shared runtime seam instead of placeholders
- [ ] The native plugin keeps the shipped MikoshiTracker read tool vocabulary without OpenClaw-only aliases
- [ ] Existing MCP read regressions stay green while shared code is reused
- [ ] Mutation tools remain deferred to Plan 03
</verification>

<success_criteria>

- OpenClaw-native read behavior now matches the shipped MikoshiTracker read intent surface
- The shared runtime seam proves itself on lower-risk read operations before mutations widen scope
- The plugin bootstrap remains stable while real handlers replace placeholders
</success_criteria>

<output>
After completion, create `.planning/phases/27-shared-api-adapter-and-native-tool-catalog/27-02-SUMMARY.md`
</output>
