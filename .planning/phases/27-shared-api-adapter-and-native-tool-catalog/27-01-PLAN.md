---
phase: 27-shared-api-adapter-and-native-tool-catalog
plan: 01
type: execute
wave: 1
depends_on:
  - 26-03
files_modified:
  - packages/mcp/src/client/api-client.ts
  - packages/mcp/src/client/errors.ts
  - packages/mcp/src/tools/inventory.ts
  - packages/openclaw-plugin/src/index.ts
  - packages/openclaw-plugin/src/register-tools.ts
  - packages/openclaw-plugin/test/shared-runtime.test.ts
  - packages/openclaw-plugin/test/tool-registration.test.ts
  - packages/mcp/test/client/api-client.test.ts
  - packages/mcp/test/client/errors.test.ts
autonomous: true
requirements:
  - SHRD-01
  - SHRD-03
user_setup: []
must_haves:
  truths:
    - The native plugin must reuse the shipped MikoshiTracker API client, contracts, and auth semantics instead of re-implementing request logic in `packages/openclaw-plugin`.
    - The reusable runtime seam must not force the native plugin to import MCP-only result types or serializers.
    - Extracting or exposing shared runtime code must not break the shipped MCP package.
  artifacts:
    - A host-neutral runtime seam for API transport and error/core tool execution reuse
    - Native plugin wiring that consumes the shared seam instead of only phase placeholders
    - Tests that pin reuse behavior and catch MCP/runtime drift
  key_links:
    - Do not duplicate endpoint paths, auth header logic, or schema parsing in the OpenClaw package.
    - Do not move MCP-only `CallToolResult` serialization into the native plugin path.
    - Do not widen this plan into implementing all read/write handlers yet.
---

<objective>
Create the shared runtime seam Phase 27 needs so OpenClaw-native tools can reuse the shipped MikoshiTracker API/client/contracts stack.

Purpose: expose or extract the host-neutral API transport, error core, and tool-operation building blocks without duplicating business logic and without pulling MCP result wrappers into the plugin path.
Output: a stable shared runtime seam that both MCP and the native plugin can consume, with placeholder-only native registration removed as the default wiring path.
</objective>

<execution_context>
@/Users/finn/.codex/get-shit-done/workflows/execute-plan.md
@/Users/finn/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-shared-api-adapter-and-native-tool-catalog/27-CONTEXT.md
@.planning/phases/27-shared-api-adapter-and-native-tool-catalog/27-RESEARCH.md
@.planning/phases/27-shared-api-adapter-and-native-tool-catalog/27-VALIDATION.md
@packages/mcp/src/client/api-client.ts
@packages/mcp/src/client/errors.ts
@packages/mcp/src/tools/inventory.ts
@packages/openclaw-plugin/src/index.ts
@packages/openclaw-plugin/src/register-tools.ts
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract or expose a host-neutral API/error runtime seam the native plugin can import safely</name>
  <files>packages/mcp/src/client/api-client.ts, packages/mcp/src/client/errors.ts, packages/mcp/src/tools/inventory.ts, packages/openclaw-plugin/test/shared-runtime.test.ts, packages/mcp/test/client/api-client.test.ts, packages/mcp/test/client/errors.test.ts</files>
  <action>Follow TDD. Start by writing a failing plugin-side test that proves the OpenClaw-native package can import the shared API transport and error/core semantics without importing MCP result wrappers. Then implement the smallest extraction or exposure seam needed to satisfy that test while preserving the existing MCP client/error behavior. Keep the seam focused on transport, parsed error semantics, and tool metadata/operation inputs, not on host-specific serialization.</action>
  <verify>
    <automated>pnpm typecheck && pnpm --filter @mikoshi-tracker/openclaw-plugin exec vitest run test/shared-runtime.test.ts && pnpm --filter @mikoshi-tracker/mcp exec vitest run test/client/api-client.test.ts test/client/errors.test.ts</automated>
  </verify>
  <done>The native plugin can consume the shipped MikoshiTracker runtime seam directly without importing MCP-only result types.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire native plugin handler injection to use the shared runtime seam instead of phase placeholders</name>
  <files>packages/openclaw-plugin/src/index.ts, packages/openclaw-plugin/src/register-tools.ts, packages/openclaw-plugin/test/tool-registration.test.ts, packages/openclaw-plugin/test/shared-runtime.test.ts</files>
  <action>Follow TDD. Add failing tests that prove plugin activation now prepares real shared-runtime-backed handlers rather than relying on `PHASE_27_PENDING` placeholders as the default path. Implement the smallest wiring needed so the registration path is ready for Plans 02 and 03 to drop in read/write behavior on top of the shared runtime seam, while keeping the native registration contract itself unchanged.</action>
  <verify>
    <automated>pnpm typecheck && pnpm --filter @mikoshi-tracker/openclaw-plugin exec vitest run test/shared-runtime.test.ts test/tool-registration.test.ts test/plugin-bootstrap.test.ts && pnpm --filter @mikoshi-tracker/mcp exec vitest run test/client/api-client.test.ts test/client/errors.test.ts</automated>
  </verify>
  <done>The plugin bootstrap is now wired around a real shared runtime seam, with placeholders no longer acting as the native default architecture.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm typecheck && pnpm --filter @mikoshi-tracker/openclaw-plugin exec vitest run test/shared-runtime.test.ts test/tool-registration.test.ts test/plugin-bootstrap.test.ts && pnpm --filter @mikoshi-tracker/mcp exec vitest run test/client/api-client.test.ts test/client/errors.test.ts` passes
- [ ] The native plugin imports shared API/auth/runtime code without importing MCP-only result wrappers
- [ ] Existing MCP client/error tests remain green after the shared seam is exposed or extracted
- [ ] Placeholder-only native registration is no longer the steady-state architecture
- [ ] Read/write execution behavior itself remains deferred to Plans 02 and 03
</verification>

<success_criteria>

- Phase 27 has one reusable runtime seam instead of two diverging host implementations
- The native plugin is structurally ready for real handlers without reopening the Phase 26 contract
- Shared runtime extraction preserves the shipped MCP behavior while unblocking the native plugin
</success_criteria>

<output>
After completion, create `.planning/phases/27-shared-api-adapter-and-native-tool-catalog/27-01-SUMMARY.md`
</output>
