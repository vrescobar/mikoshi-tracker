---
phase: 27-shared-api-adapter-and-native-tool-catalog
plan: 03
type: execute
wave: 3
depends_on:
  - 27-02
files_modified:
  - packages/openclaw-plugin/src/index.ts
  - packages/openclaw-plugin/src/register-tools.ts
  - packages/openclaw-plugin/test/mutation-tools.test.ts
  - packages/openclaw-plugin/test/plugin-bootstrap.test.ts
  - packages/openclaw-plugin/test/tool-registration.test.ts
  - packages/mcp/src/tools/habits.ts
  - packages/mcp/src/tools/today.ts
  - packages/mcp/src/client/errors.ts
  - packages/mcp/test/tools/habits-write.test.ts
  - packages/mcp/test/tools/today-write.test.ts
  - packages/mcp/test/tools/mutation-errors.test.ts
autonomous: true
requirements:
  - OCP-02
  - SHRD-01
  - SHRD-03
user_setup: []
must_haves:
  truths:
    - The native plugin must expose the shipped habit-management and today-mutation coverage through direct MikoshiTracker API handlers.
    - Native mutation handlers must use the shared runtime/auth path and preserve the same route semantics as the shipped MCP tools.
    - Mutation implementation must not copy domain rules or bypass the API's validation/conflict behavior.
  artifacts:
    - Real native handlers for habits and today mutation tools
    - Plugin tests that pin request method/path/body behavior and eliminate the remaining Phase 27 placeholders
    - Shared mutation/error reuse that keeps existing MCP regressions green
  key_links:
    - Do not fork mutation tool names, request shapes, or auth behavior for OpenClaw.
    - Do not implement archived-habit, wrong-kind, or auth rules separately in the plugin package if the shared/runtime API path already enforces them.
    - Do not widen this plan into the final native success/error envelope redesign from Phase 28.
---

<objective>
Implement the native OpenClaw mutation tools on top of the shared MikoshiTracker API runtime.

Purpose: replace the remaining native plugin placeholders for habit management and today actions with direct API-backed handlers that preserve the shipped MikoshiTracker semantics and auth behavior.
Output: functioning native mutation tools for habits and today actions, with tests that lock request parity, placeholder removal, and shared-runtime regression safety.
</objective>

<execution_context>
@/Users/finn/.codex/get-shit-done/workflows/execute-plan.md
@/Users/finn/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-shared-api-adapter-and-native-tool-catalog/27-CONTEXT.md
@.planning/phases/27-shared-api-adapter-and-native-tool-catalog/27-RESEARCH.md
@.planning/phases/27-shared-api-adapter-and-native-tool-catalog/27-VALIDATION.md
@packages/mcp/src/tools/habits.ts
@packages/mcp/src/tools/today.ts
@packages/mcp/src/client/errors.ts
@packages/openclaw-plugin/src/index.ts
@packages/openclaw-plugin/src/register-tools.ts
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement native habit mutation tools on the shared runtime seam</name>
  <files>packages/openclaw-plugin/src/index.ts, packages/openclaw-plugin/src/register-tools.ts, packages/openclaw-plugin/test/mutation-tools.test.ts, packages/mcp/src/tools/habits.ts, packages/mcp/test/tools/habits-write.test.ts</files>
  <action>Follow TDD. Start with failing native plugin tests that prove `habits_add`, `habits_edit`, `habits_archive`, and `habits_restore` call the correct MikoshiTracker API routes with the same request semantics as the shipped MCP tools and no longer return `PHASE_27_PENDING`. Implement the smallest shared mutation-operation reuse needed to satisfy those tests while keeping route logic, request parsing, and summary behavior single-sourced.</action>
  <verify>
    <automated>pnpm typecheck && pnpm --filter @mikoshi-tracker/openclaw-plugin exec vitest run test/mutation-tools.test.ts && pnpm --filter @mikoshi-tracker/mcp exec vitest run test/tools/habits-write.test.ts</automated>
  </verify>
  <done>The native plugin can now perform real habit management mutations directly against the shipped MikoshiTracker API.</done>
</task>

<task type="auto">
  <name>Task 2: Implement native today mutation tools and keep shared mutation/error semantics aligned with the shipped MCP path</name>
  <files>packages/openclaw-plugin/src/index.ts, packages/openclaw-plugin/src/register-tools.ts, packages/openclaw-plugin/test/mutation-tools.test.ts, packages/openclaw-plugin/test/plugin-bootstrap.test.ts, packages/openclaw-plugin/test/tool-registration.test.ts, packages/mcp/src/tools/today.ts, packages/mcp/src/client/errors.ts, packages/mcp/test/tools/today-write.test.ts, packages/mcp/test/tools/mutation-errors.test.ts</files>
  <action>Follow TDD. Add failing native plugin tests that prove `today_complete`, `today_set_total`, and `today_undo` execute through the shared runtime/auth seam with the same endpoint and wrong-kind/not-found/error behavior assumptions as the shipped MCP implementation, while the plugin bootstrap continues to register the same full catalog. Implement only the minimal shared-runtime-backed mutation wiring and shared error-core reuse needed to make those tests pass.</action>
  <verify>
    <automated>pnpm typecheck && pnpm --filter @mikoshi-tracker/openclaw-plugin build && pnpm --filter @mikoshi-tracker/openclaw-plugin exec vitest run test/mutation-tools.test.ts test/plugin-bootstrap.test.ts test/tool-registration.test.ts && pnpm --filter @mikoshi-tracker/mcp exec vitest run test/tools/today-write.test.ts test/tools/mutation-errors.test.ts</automated>
  </verify>
  <done>The native plugin now exposes the full shipped habits/today mutation surface through direct API-backed handlers, with remaining envelope hardening deferred to Phase 28.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm typecheck && pnpm --filter @mikoshi-tracker/openclaw-plugin build && pnpm --filter @mikoshi-tracker/openclaw-plugin exec vitest run test/mutation-tools.test.ts test/read-tools.test.ts test/plugin-bootstrap.test.ts test/tool-registration.test.ts && pnpm --filter @mikoshi-tracker/mcp exec vitest run test/tools/habits-write.test.ts test/tools/today-write.test.ts test/tools/mutation-errors.test.ts` passes
- [ ] All native habit and today mutation tools use the shared runtime seam instead of placeholder handlers
- [ ] Request method/path/body semantics stay aligned with the shipped MCP behavior
- [ ] Existing MCP mutation/error regressions remain green after shared reuse changes
- [ ] Final native success/error envelope work remains deferred to Phase 28
</verification>

<success_criteria>

- OpenClaw-native handlers now cover the full shipped MikoshiTracker habits/today/stats tool vocabulary
- Shared API/auth/runtime logic remains single-sourced instead of being copied into the plugin
- Phase 28 can focus on envelope/error hardening and operator guidance, not on basic handler existence
</success_criteria>

<output>
After completion, create `.planning/phases/27-shared-api-adapter-and-native-tool-catalog/27-03-SUMMARY.md`
</output>
