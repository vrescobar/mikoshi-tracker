---
phase: 26-native-plugin-contract-and-package-scaffold
plan: 01
type: execute
wave: 1
depends_on:
  - 25-02
files_modified:
  - packages/openclaw-plugin/package.json
  - packages/openclaw-plugin/openclaw.plugin.json
  - packages/openclaw-plugin/src/index.ts
  - packages/openclaw-plugin/tsup.config.ts
  - packages/openclaw-plugin/vitest.config.ts
  - packages/openclaw-plugin/test/plugin-manifest.test.ts
  - packages/openclaw-plugin/test/plugin-bootstrap.test.ts
autonomous: true
requirements:
  - OCP-01
  - OCP-03
user_setup: []
must_haves:
  truths:
    - Phase 26 must create a real OpenClaw-native package, not a wrapper that boots the existing MCP runtime behind the scenes.
    - The native plugin contract should be anchored on `openclaw.plugin.json` plus a bootstrap entry that registers tools through the host plugin API.
    - This plan sets the package contract and build/test surface only; it must not widen into full domain handler implementation.
  artifacts:
    - A new `packages/openclaw-plugin` workspace package with native manifest and bootstrap entry
    - Contract smoke tests that lock the manifest/bootstrap shape and fail if MCP bridging sneaks back in
    - Package-local build/test config so the plugin behaves like a first-class monorepo package
  key_links:
    - Do not spawn `@mikoshi-tracker/mcp`, `mcporter`, or an MCP server from the plugin bootstrap.
    - Do not invent a second MikoshiTracker tool vocabulary for OpenClaw.
    - Do not move full handler implementation into this plan.
---

<objective>
Establish the native OpenClaw plugin contract and package scaffold for MikoshiTracker.

Purpose: give OpenClaw a first-class MikoshiTracker plugin package that it can load directly, with the correct manifest, bootstrap entrypoint, and monorepo build/test shape.
Output: `packages/openclaw-plugin` exists as a native plugin package with smoke-tested manifest/bootstrap behavior and no hidden MCP dependency.
</objective>

<execution_context>
@/Users/finn/.codex/get-shit-done/workflows/execute-plan.md
@/Users/finn/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-native-plugin-contract-and-package-scaffold/26-CONTEXT.md
@.planning/phases/26-native-plugin-contract-and-package-scaffold/26-RESEARCH.md
@.planning/phases/26-native-plugin-contract-and-package-scaffold/26-VALIDATION.md
@packages/mcp/package.json
@packages/mcp/src/server/create-server.ts
@packages/mcp/src/tools/inventory.ts
@docs/ai-agent-integration.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock the OpenClaw-native manifest and bootstrap contract with failing tests before creating the new package</name>
  <files>packages/openclaw-plugin/test/plugin-manifest.test.ts, packages/openclaw-plugin/test/plugin-bootstrap.test.ts, packages/openclaw-plugin/openclaw.plugin.json, packages/openclaw-plugin/src/index.ts</files>
  <action>Follow TDD. Start by adding failing tests that assert the new package ships an `openclaw.plugin.json` manifest and a bootstrap entry that uses the native OpenClaw plugin API to register tools, while explicitly proving it does not spawn or embed the existing MCP runtime. Then add the smallest manifest and bootstrap files needed to satisfy those tests, keeping the tool registration surface skeletal if real handlers are not yet implemented.</action>
  <verify>
    <automated>pnpm typecheck && pnpm --filter @mikoshi-tracker/openclaw-plugin exec vitest run test/plugin-manifest.test.ts test/plugin-bootstrap.test.ts</automated>
  </verify>
  <done>MikoshiTracker has a native OpenClaw plugin contract on disk, and smoke tests prove the bootstrap path is not just MCP in disguise.</done>
</task>

<task type="auto">
  <name>Task 2: Make the native plugin package buildable and testable as a first-class workspace package</name>
  <files>packages/openclaw-plugin/package.json, packages/openclaw-plugin/tsup.config.ts, packages/openclaw-plugin/vitest.config.ts, packages/openclaw-plugin/src/index.ts</files>
  <action>Follow TDD. Add the minimal package metadata and config needed so the new plugin participates cleanly in the existing `packages/*` workspace: a package name, Node/runtime expectations, build entry, and package-local Vitest/Tsup setup. Keep the implementation thin and focused on plugin bootstrapping only; do not pull full MikoshiTracker API behavior into this plan.</action>
  <verify>
    <automated>pnpm typecheck && pnpm --filter @mikoshi-tracker/openclaw-plugin build && pnpm --filter @mikoshi-tracker/openclaw-plugin exec vitest run test/plugin-manifest.test.ts test/plugin-bootstrap.test.ts</automated>
  </verify>
  <done>The repo can build and test `packages/openclaw-plugin` like any other first-class package, giving later plans a stable surface to build on.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm typecheck && pnpm --filter @mikoshi-tracker/openclaw-plugin exec vitest run test/plugin-manifest.test.ts test/plugin-bootstrap.test.ts` passes
- [ ] `pnpm --filter @mikoshi-tracker/openclaw-plugin build` succeeds
- [ ] `packages/openclaw-plugin/openclaw.plugin.json` exists and describes a native plugin contract
- [ ] The plugin bootstrap does not spawn `@mikoshi-tracker/mcp`, `mcporter`, or another MCP server process
- [ ] The new package is scaffolded without widening into full tool handler implementation
</verification>

<success_criteria>

- MikoshiTracker now has a real OpenClaw-native package scaffold instead of only a skill-plus-MCP story
- The native plugin contract is testable and guarded against MCP fallback regressions
- Plan 02 can focus on tool catalog registration rather than package plumbing
</success_criteria>

<output>
After completion, create `.planning/phases/26-native-plugin-contract-and-package-scaffold/26-01-SUMMARY.md`
</output>
