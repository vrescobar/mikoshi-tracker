---
phase: 26-native-plugin-contract-and-package-scaffold
plan: 03
type: execute
wave: 3
depends_on:
  - 26-02
files_modified:
  - packages/openclaw-plugin/src/config/env.ts
  - packages/openclaw-plugin/src/errors.ts
  - packages/openclaw-plugin/src/index.ts
  - packages/openclaw-plugin/test/config/env.test.ts
  - packages/openclaw-plugin/test/plugin-startup-errors.test.ts
autonomous: true
requirements:
  - SHRD-02
user_setup: []
must_haves:
  truths:
    - The native plugin runtime contract is exactly `MIKOSHI_TRACKER_API_URL` plus `MIKOSHI_TRACKER_API_TOKEN`.
    - Startup should fail before any API call if config is missing, empty, malformed, or obviously unusable.
    - Error messaging must stay actionable and redact token values rather than leaking secrets into logs or host surfaces.
  artifacts:
    - A package-local env parser/validator for the native plugin
    - Structured startup/config error types that later native tools can reuse
    - Tests that lock exact env names, redaction behavior, and fail-fast diagnostics
  key_links:
    - Do not accept passwords, alternate token env names, or MCP-specific config flags.
    - Do not defer obvious config errors until the first tool invocation.
    - Do not couple core config errors to MCP-only result wrappers.
---

<objective>
Add strict runtime config validation and startup diagnostics to the native OpenClaw plugin scaffold.

Purpose: make the new plugin fail fast and clearly when `MIKOSHI_TRACKER_API_URL` or `MIKOSHI_TRACKER_API_TOKEN` is wrong, so later handler work starts from a safe runtime foundation.
Output: exact env parsing, redaction-safe startup errors, and tests that prove the native plugin reports config problems clearly.
</objective>

<execution_context>
@/Users/finn/.codex/get-shit-done/workflows/execute-plan.md
@/Users/finn/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-native-plugin-contract-and-package-scaffold/26-CONTEXT.md
@.planning/phases/26-native-plugin-contract-and-package-scaffold/26-RESEARCH.md
@.planning/phases/26-native-plugin-contract-and-package-scaffold/26-VALIDATION.md
@packages/mcp/src/config/env.ts
@packages/mcp/src/client/errors.ts
@packages/openclaw-plugin/openclaw.plugin.json
@packages/openclaw-plugin/src/index.ts
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement strict env parsing for `MIKOSHI_TRACKER_API_URL` and `MIKOSHI_TRACKER_API_TOKEN` with actionable validation failures</name>
  <files>packages/openclaw-plugin/src/config/env.ts, packages/openclaw-plugin/test/config/env.test.ts</files>
  <action>Follow TDD. Start by adding failing env tests that cover missing values, empty strings, malformed URL input, and obviously bad token input, always referencing the exact env names the plugin requires. Then implement the smallest package-local env parser that satisfies those cases and mirrors the already-shipped token-oriented runtime contract without importing MCP-only config code wholesale.</action>
  <verify>
    <automated>pnpm typecheck && pnpm --filter @mikoshi-tracker/openclaw-plugin exec vitest run test/config/env.test.ts</automated>
  </verify>
  <done>The native plugin now validates its runtime contract explicitly instead of waiting for a later API call to fail vaguely.</done>
</task>

<task type="auto">
  <name>Task 2: Surface redaction-safe startup/config errors from the native plugin bootstrap</name>
  <files>packages/openclaw-plugin/src/errors.ts, packages/openclaw-plugin/src/index.ts, packages/openclaw-plugin/test/plugin-startup-errors.test.ts</files>
  <action>Follow TDD. Add failing startup tests that prove the plugin returns clear structured configuration errors from bootstrap time, redacts token values, and avoids leaking MCP-specific error wrappers. Then implement the smallest error core and bootstrap wiring needed so later phases can reuse the same startup/config failure shape when real handlers arrive.</action>
  <verify>
    <automated>pnpm typecheck && pnpm --filter @mikoshi-tracker/openclaw-plugin exec vitest run test/config/env.test.ts test/plugin-startup-errors.test.ts</automated>
  </verify>
  <done>The native plugin bootstrap now fails fast with explicit, redaction-safe diagnostics that future tool handlers can build on.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm typecheck && pnpm --filter @mikoshi-tracker/openclaw-plugin exec vitest run test/config/env.test.ts test/plugin-startup-errors.test.ts` passes
- [ ] Startup errors reference `MIKOSHI_TRACKER_API_URL` and `MIKOSHI_TRACKER_API_TOKEN` exactly
- [ ] Token values are redacted from messages and thrown error payloads
- [ ] The bootstrap fails before any API request when config is invalid
- [ ] Config/error logic is host-neutral enough that Phase 27 can reuse it without importing MCP result types
</verification>

<success_criteria>

- The native plugin has a strict runtime contract before any real handler work lands
- Operators get immediate, actionable configuration feedback instead of vague downstream failures
- Phase 27 can focus on API-backed handlers because the package bootstrap and diagnostics are already stable
</success_criteria>

<output>
After completion, create `.planning/phases/26-native-plugin-contract-and-package-scaffold/26-03-SUMMARY.md`
</output>
