---
phase: 26-native-plugin-contract-and-package-scaffold
plan: 02
type: execute
wave: 2
depends_on:
  - 26-01
files_modified:
  - packages/openclaw-plugin/src/tool-catalog.ts
  - packages/openclaw-plugin/src/register-tools.ts
  - packages/openclaw-plugin/src/index.ts
  - packages/openclaw-plugin/test/tool-catalog.test.ts
  - packages/openclaw-plugin/test/tool-registration.test.ts
  - packages/mcp/src/tools/inventory.ts
autonomous: true
requirements:
  - OCP-01
  - OCP-03
user_setup: []
must_haves:
  truths:
    - The OpenClaw-native package must expose the same habits/today/stats vocabulary that MikoshiTracker already ships, not a forked host-specific naming scheme.
    - Catalog reuse should come from shared metadata and schemas, not by copying tool definitions into the new package.
    - If Phase 26 stops at a registration shell, placeholder handlers must stay explicitly native and must not route through MCP.
  artifacts:
    - A host-native MikoshiTracker tool catalog shape for the plugin package
    - Native registration wiring that exposes the intended tool names and schemas to OpenClaw
    - Tests that prove the plugin catalog stays aligned with the shipped MikoshiTracker vocabulary
  key_links:
    - Do not wire registration by spawning or proxying the MCP server.
    - Do not duplicate tool names, schemas, or descriptions by hand if shared metadata can be extracted or referenced.
    - Do not promise full API-backed behavior in this plan if the real handlers are intentionally deferred to Phase 27.
---

<objective>
Create the native OpenClaw tool catalog shell and registration wiring for MikoshiTracker.

Purpose: make the new plugin package expose the intended MikoshiTracker habits/today/stats tool surface natively, using shared metadata rather than copied definitions.
Output: OpenClaw-native tool registration exists for the MikoshiTracker catalog, with tests that pin vocabulary and forbid MCP fallback.
</objective>

<execution_context>
@/Users/finn/.codex/get-shit-done/workflows/execute-plan.md
@/Users/finn/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-native-plugin-contract-and-package-scaffold/26-CONTEXT.md
@.planning/phases/26-native-plugin-contract-and-package-scaffold/26-RESEARCH.md
@.planning/phases/26-native-plugin-contract-and-package-scaffold/26-VALIDATION.md
@packages/mcp/src/tools/inventory.ts
@packages/mcp/src/tools/habits.ts
@packages/mcp/src/tools/today.ts
@packages/mcp/src/tools/stats.ts
@packages/mcp/src/client/errors.ts
@packages/openclaw-plugin/package.json
@packages/openclaw-plugin/openclaw.plugin.json
@packages/openclaw-plugin/src/index.ts
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define a host-native MikoshiTracker tool catalog that reuses the shipped vocabulary and shared metadata</name>
  <files>packages/openclaw-plugin/src/tool-catalog.ts, packages/openclaw-plugin/test/tool-catalog.test.ts, packages/mcp/src/tools/inventory.ts</files>
  <action>Follow TDD. Start by writing failing catalog tests that require the OpenClaw-native package to expose the same top-level MikoshiTracker tool names and core schemas for habits, today, and stats that the repo already ships. Then implement the smallest shared catalog seam needed to satisfy those checks, preferring reuse or extraction over copy-paste. Keep the seam host-neutral so the MCP package can continue consuming it safely.</action>
  <verify>
    <automated>pnpm typecheck && pnpm --filter @mikoshi-tracker/openclaw-plugin exec vitest run test/tool-catalog.test.ts</automated>
  </verify>
  <done>The OpenClaw-native package has a MikoshiTracker tool catalog that stays aligned with existing tool vocabulary and schemas.</done>
</task>

<task type="auto">
  <name>Task 2: Register the native MikoshiTracker tool catalog through the OpenClaw plugin API without routing through MCP</name>
  <files>packages/openclaw-plugin/src/register-tools.ts, packages/openclaw-plugin/src/index.ts, packages/openclaw-plugin/test/tool-registration.test.ts</files>
  <action>Follow TDD. Add failing registration tests that prove the plugin bootstrap exposes the full planned MikoshiTracker catalog through native OpenClaw registration calls and does not invoke an MCP server in order to do so. Then wire the plugin bootstrap to register the catalog. If real handlers are deferred to Phase 27, return explicit temporary placeholders that make the deferral visible but still keep registration fully native.</action>
  <verify>
    <automated>pnpm typecheck && pnpm --filter @mikoshi-tracker/openclaw-plugin exec vitest run test/plugin-bootstrap.test.ts test/tool-registration.test.ts</automated>
  </verify>
  <done>The OpenClaw-native package now surfaces the intended MikoshiTracker tool catalog directly through host-native registration instead of through MCP transport.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm typecheck && pnpm --filter @mikoshi-tracker/openclaw-plugin exec vitest run test/tool-catalog.test.ts test/tool-registration.test.ts test/plugin-bootstrap.test.ts` passes
- [ ] The plugin exposes the habits/today/stats vocabulary without an OpenClaw-only naming fork
- [ ] Shared catalog metadata is reused or extracted instead of copied manually into the new package
- [ ] Tool registration stays native even if actual domain handlers are still placeholders
- [ ] No MCP server bootstrap or subprocess is required for registration
</verification>

<success_criteria>

- OpenClaw can see the planned MikoshiTracker-native tool catalog from the new package
- The repo has one path toward shared tool metadata instead of duplicated host-specific definitions
- Plan 03 can focus only on env validation and startup diagnostics
</success_criteria>

<output>
After completion, create `.planning/phases/26-native-plugin-contract-and-package-scaffold/26-02-SUMMARY.md`
</output>
