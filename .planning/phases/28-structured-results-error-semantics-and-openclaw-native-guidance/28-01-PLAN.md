---
phase: 28-structured-results-error-semantics-and-openclaw-native-guidance
plan: 01
type: execute
wave: 1
depends_on:
  - 27-03
files_modified:
  - packages/openclaw-plugin/src/native-handlers.ts
  - packages/openclaw-plugin/src/types.ts
  - packages/openclaw-plugin/test/result-envelope.test.ts
  - packages/openclaw-plugin/test/read-tools.test.ts
  - packages/openclaw-plugin/test/mutation-tools.test.ts
autonomous: true
requirements:
  - RESP-01
user_setup: []
must_haves:
  truths:
    - Every native plugin tool must return one stable machine-readable success shape rather than tool-specific ad hoc objects.
    - Success responses must not expose raw HTTP response objects or transport-specific wrappers.
    - The locked success contract must work the same way for both read tools and mutation tools.
  artifacts:
    - A final native success envelope in `packages/openclaw-plugin`
    - Dedicated result-envelope tests that pin the contract across read and mutation tools
    - Updated plugin typings reflecting the hardened success shape
  key_links:
    - Do not move OpenClaw-specific envelope concerns back into the shared runtime seam.
    - Do not widen this plan into full error taxonomy redesign or docs migration.
    - Do not make agents depend on parsing `summary` text to understand success payloads.
---

<objective>
Define and implement the final native plugin success envelope.

Purpose: make every OpenClaw-native tool return one stable structured JSON success shape that agents can consume directly without scraping text or reasoning about host transport wrappers.
Output: a locked native success envelope, dedicated envelope coverage, and updated read/mutation tests aligned to that contract.
</objective>

<execution_context>
@/Users/finn/.codex/get-shit-done/workflows/execute-plan.md
@/Users/finn/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-structured-results-error-semantics-and-openclaw-native-guidance/28-CONTEXT.md
@.planning/phases/28-structured-results-error-semantics-and-openclaw-native-guidance/28-RESEARCH.md
@.planning/phases/28-structured-results-error-semantics-and-openclaw-native-guidance/28-VALIDATION.md
@packages/openclaw-plugin/src/native-handlers.ts
@packages/openclaw-plugin/test/read-tools.test.ts
@packages/openclaw-plugin/test/mutation-tools.test.ts
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock one native success envelope across read and mutation tools</name>
  <files>packages/openclaw-plugin/src/native-handlers.ts, packages/openclaw-plugin/src/types.ts, packages/openclaw-plugin/test/result-envelope.test.ts, packages/openclaw-plugin/test/read-tools.test.ts, packages/openclaw-plugin/test/mutation-tools.test.ts</files>
  <action>Follow TDD. Start with failing tests that assert one stable success contract for both read and mutation tools, with enough machine-readable structure that an agent can rely on JSON fields instead of scraping the summary text. Then implement the smallest envelope and typing changes needed to satisfy those tests while preserving the Phase 27 runtime behavior and keeping the serialization boundary inside `packages/openclaw-plugin`.</action>
  <verify>
    <automated>pnpm typecheck && pnpm --filter @mikoshi-tracker/openclaw-plugin exec vitest run test/result-envelope.test.ts test/read-tools.test.ts test/mutation-tools.test.ts</automated>
  </verify>
  <done>The native plugin now has one stable success envelope instead of a merely convenient implementation shape.</done>
</task>

<task type="auto">
  <name>Task 2: Align plugin bootstrap and native typings to the final success envelope without reopening handler coverage</name>
  <files>packages/openclaw-plugin/src/native-handlers.ts, packages/openclaw-plugin/src/types.ts, packages/openclaw-plugin/test/result-envelope.test.ts, packages/openclaw-plugin/test/read-tools.test.ts, packages/openclaw-plugin/test/mutation-tools.test.ts</files>
  <action>Follow TDD. Add failing tests that prove the hardened success envelope is the steady-state contract from the plugin's public surface, not just an incidental internal implementation detail. Update only the minimal native typings and bootstrap-facing expectations needed to make that contract explicit and durable, without widening the plan into error taxonomy or docs work.</action>
  <verify>
    <automated>pnpm typecheck && pnpm --filter @mikoshi-tracker/openclaw-plugin exec vitest run test/result-envelope.test.ts test/plugin-bootstrap.test.ts test/tool-registration.test.ts test/read-tools.test.ts test/mutation-tools.test.ts</automated>
  </verify>
  <done>The final native success contract is explicit, typed, and protected against future drift.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm typecheck && pnpm --filter @mikoshi-tracker/openclaw-plugin exec vitest run test/result-envelope.test.ts test/plugin-bootstrap.test.ts test/tool-registration.test.ts test/read-tools.test.ts test/mutation-tools.test.ts` passes
- [ ] Read and mutation tools now share one stable success envelope
- [ ] Machine-readable success fields are sufficient without parsing `summary`
- [ ] The hardened envelope remains inside the native plugin boundary instead of leaking into the shared runtime seam
- [ ] Error taxonomy and docs migration remain deferred to Plans 02 and 03
</verification>

<success_criteria>

- Agents can consume native success results through one predictable JSON contract
- The native plugin contract is explicit enough that later phases do not need to infer success shape from tests
- Phase 28 can build sharper error semantics on top of a locked success envelope
</success_criteria>

<output>
After completion, create `.planning/phases/28-structured-results-error-semantics-and-openclaw-native-guidance/28-01-SUMMARY.md`
</output>
