---
phase: 28-structured-results-error-semantics-and-openclaw-native-guidance
plan: 02
type: execute
wave: 2
depends_on:
  - 28-01
files_modified:
  - packages/openclaw-plugin/src/native-handlers.ts
  - packages/openclaw-plugin/src/errors.ts
  - packages/mcp/src/client/error-payload.ts
  - packages/openclaw-plugin/test/error-semantics.test.ts
  - packages/openclaw-plugin/test/mutation-tools.test.ts
  - packages/openclaw-plugin/test/plugin-bootstrap.test.ts
  - packages/openclaw-plugin/test/tool-registration.test.ts
  - packages/mcp/test/client/errors.test.ts
  - packages/mcp/test/tools/mutation-errors.test.ts
autonomous: true
requirements:
  - RESP-02
  - RESP-03
user_setup: []
must_haves:
  truths:
    - Native plugin failures must clearly distinguish connectivity/timeouts from authenticated API rejections.
    - Missing targets and wrong-kind mutation/tool mismatches must tell the agent whether it needs a different `habitId` or a different tool.
    - Error semantics should stay aligned with the shared MikoshiTracker runtime while exposing a sharper native contract.
  artifacts:
    - A hardened native plugin error taxonomy and payload shape
    - Dedicated error-semantics tests for timeout/network/auth/not-found/wrong-kind cases
    - Updated mutation/error regression coverage for the native plugin and shared seam
  key_links:
    - Do not collapse all failures back into generic `validation` or `unknown` buckets.
    - Do not force agents to parse English prose to distinguish wrong-kind vs not-found vs auth failures.
    - Do not widen this plan into docs migration or full repository verification.
---

<objective>
Map native plugin failures into stable, actionable error semantics.

Purpose: harden the OpenClaw-native error contract so agents can branch reliably on config, network, auth, not-found, and wrong-tool/type failures without scraping messages.
Output: a stable native error payload and category taxonomy with tests that pin the important agent retry/fallback scenarios.
</objective>

<execution_context>
@/Users/finn/.codex/get-shit-done/workflows/execute-plan.md
@/Users/finn/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-structured-results-error-semantics-and-openclaw-native-guidance/28-CONTEXT.md
@.planning/phases/28-structured-results-error-semantics-and-openclaw-native-guidance/28-RESEARCH.md
@.planning/phases/28-structured-results-error-semantics-and-openclaw-native-guidance/28-VALIDATION.md
@packages/openclaw-plugin/src/native-handlers.ts
@packages/openclaw-plugin/src/errors.ts
@packages/mcp/src/client/error-payload.ts
@packages/openclaw-plugin/test/mutation-tools.test.ts
@packages/mcp/test/tools/mutation-errors.test.ts
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define a stable native error category contract for timeout/network/auth/not-found/wrong-kind failures</name>
  <files>packages/openclaw-plugin/src/native-handlers.ts, packages/openclaw-plugin/src/errors.ts, packages/mcp/src/client/error-payload.ts, packages/openclaw-plugin/test/error-semantics.test.ts, packages/mcp/test/client/errors.test.ts, packages/mcp/test/tools/mutation-errors.test.ts</files>
  <action>Follow TDD. Start with failing plugin-side tests that assert stable machine-readable categories and actionable hints for timeout/network failures, authenticated API rejections, missing habits, and wrong-kind tool mismatches. Then implement the smallest native error taxonomy and shared-error-seam adjustments needed to satisfy those tests while keeping the underlying runtime semantics single-sourced.</action>
  <verify>
    <automated>pnpm typecheck && pnpm --filter @mikoshi-tracker/openclaw-plugin exec vitest run test/error-semantics.test.ts && pnpm --filter @mikoshi-tracker/mcp exec vitest run test/client/errors.test.ts test/tools/mutation-errors.test.ts</automated>
  </verify>
  <done>The native plugin exposes one explicit failure taxonomy that agents can branch on safely.</done>
</task>

<task type="auto">
  <name>Task 2: Align native mutation/bootstrap regressions to the final error semantics without weakening shared runtime behavior</name>
  <files>packages/openclaw-plugin/src/native-handlers.ts, packages/openclaw-plugin/test/error-semantics.test.ts, packages/openclaw-plugin/test/mutation-tools.test.ts, packages/openclaw-plugin/test/plugin-bootstrap.test.ts, packages/openclaw-plugin/test/tool-registration.test.ts</files>
  <action>Follow TDD. Add failing tests that prove the hardened native error contract is what the plugin actually emits on real mutation failure paths and bootstrap/runtime boundaries, not just what the standalone mapper would ideally return. Implement only the minimal native-layer changes needed to make those real-path tests pass while preserving the shared runtime and current handler behavior.</action>
  <verify>
    <automated>pnpm typecheck && pnpm --filter @mikoshi-tracker/openclaw-plugin exec vitest run test/error-semantics.test.ts test/mutation-tools.test.ts test/plugin-bootstrap.test.ts test/tool-registration.test.ts && pnpm --filter @mikoshi-tracker/mcp exec vitest run test/client/errors.test.ts test/tools/mutation-errors.test.ts</automated>
  </verify>
  <done>The native plugin now emits stable actionable failures on the real code paths agents will hit.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm typecheck && pnpm --filter @mikoshi-tracker/openclaw-plugin exec vitest run test/error-semantics.test.ts test/mutation-tools.test.ts test/plugin-bootstrap.test.ts test/tool-registration.test.ts && pnpm --filter @mikoshi-tracker/mcp exec vitest run test/client/errors.test.ts test/tools/mutation-errors.test.ts` passes
- [ ] Native failures clearly distinguish network/timeout, auth, not-found, and wrong-kind/tool scenarios
- [ ] Error payloads are machine-branchable without parsing English-only message text
- [ ] Native error hardening stays aligned with the shared runtime rather than duplicating domain rules
- [ ] Docs migration remains deferred to Plan 03
</verification>

<success_criteria>

- Agents can retry or reroute native plugin calls based on stable failure categories
- Wrong-kind and not-found failures become explicit enough to prevent avoidable agent loops
- The native plugin contract is now strong enough to document as the OpenClaw default path
</success_criteria>

<output>
After completion, create `.planning/phases/28-structured-results-error-semantics-and-openclaw-native-guidance/28-02-SUMMARY.md`
</output>
