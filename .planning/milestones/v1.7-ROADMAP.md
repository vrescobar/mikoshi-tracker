# Milestone v1.7: OpenClaw Native Plugin

**Status:** ✅ SHIPPED 2026-03-11
**Phases:** 26-29
**Total Plans:** 11

## Overview

v1.7 replaced the previous OpenClaw `skill -> mcporter -> MCP -> API` chain with a native OpenClaw plugin package that talks directly to the shipped MikoshiTracker API. The milestone kept the MikoshiTracker domain model, bearer-token auth contract, and shared client/types single-sourced while removing transport indirection for the host that most needed native tool availability.

## Phases

### Phase 26: Native Plugin Contract and Package Scaffold

**Goal**: Define the OpenClaw-native plugin contract and add the new package/runtime scaffold without reintroducing MCP as an internal dependency.
**Depends on**: Shipped v1.6 OpenClaw compatibility baseline and current MikoshiTracker API contracts
**Plans**: 3 plans

Plans:

- [x] 26-01: Confirm the OpenClaw plugin manifest/runtime contract, package entrypoints, and workspace integration points for a native tool package
- [x] 26-02: Add `packages/openclaw-plugin` with build/package scaffolding plus the manifest and bootstrap entrypoints OpenClaw expects
- [x] 26-03: Implement plugin startup config and fail-fast env validation around `MIKOSHI_TRACKER_API_URL` and `MIKOSHI_TRACKER_API_TOKEN` without routing through MCP config loaders

**Details:**
- Requirements: `OCP-01`, `OCP-03`, `SHRD-02`
- Outcomes: one discoverable plugin package, one explicit runtime contract, and zero MCP bridge dependency in the OpenClaw-native path.
- Success criteria:
  1. The repo contains a first-class OpenClaw plugin package that is structurally ready to register native tools.
  2. Plugin startup tells the operator exactly what is wrong when API URL/token config is absent or unusable.
  3. The native plugin scaffold does not wrap or spawn the MCP server as an implementation crutch.

### Phase 27: Shared API Adapter and Native Tool Catalog

**Goal**: Reuse the shipped API/client/contracts stack to expose the full MikoshiTracker tool catalog as native OpenClaw tools.
**Depends on**: Phase 26
**Plans**: 3 plans

Plans:

- [x] 27-01: Extract or expose the reusable API client/contracts/error primitives needed by the plugin without duplicating domain logic
- [x] 27-02: Implement native OpenClaw read tools for habits, today summary, and stats overview on top of the shared API layer
- [x] 27-03: Implement native OpenClaw mutation tools for habit management and today actions with the same bearer-authenticated API semantics

**Details:**
- Requirements: `OCP-02`, `SHRD-01`, `SHRD-03`
- Outcomes: one native OpenClaw tool surface backed directly by the shipped MikoshiTracker API, with shared contracts instead of a forked OpenClaw-specific implementation.
- Success criteria:
  1. The plugin exposes the intended habits/today/stats capability set without inventing a second domain vocabulary.
  2. Shared client/types/auth code is reused rather than copied into `packages/openclaw-plugin`.
  3. Tool handlers talk directly to the MikoshiTracker API and keep domain behavior single-sourced there.

### Phase 28: Structured Results, Error Semantics, and OpenClaw-native Guidance

**Goal**: Make the native plugin agent-friendly by returning stable structured JSON and explicit failure categories, then update host-facing guidance to prefer the new path.
**Depends on**: Phase 27
**Plans**: 3 plans

Plans:

- [x] 28-01: Define and implement the native plugin success envelope so every tool returns machine-readable JSON rather than raw HTTP output
- [x] 28-02: Map network failures, not-found targets, and wrong-kind mutations into clear structured plugin errors with actionable hints
- [x] 28-03: Update OpenClaw-facing docs/examples so operators follow the native plugin path first and understand how it differs from the generic MCP story

**Details:**
- Requirements: `RESP-01`, `RESP-02`, `RESP-03`, `VER-02`
- Outcomes: predictable agent-readable outputs, sharper diagnostics, and one canonical story for OpenClaw-native runtime setup.
- Success criteria:
  1. Success responses are easy for agents to consume without scraping text or HTTP wrappers.
  2. Failure responses clearly distinguish config, network, not-found, and wrong-tool/type scenarios.
  3. OpenClaw docs no longer imply that this host should prefer the older MCP-bridged path by default.

### Phase 29: Verification Gate and Migration Confidence

**Goal**: Prove the new native plugin path works in-repo and is ready to replace the previous OpenClaw bridge in normal use.
**Depends on**: Phase 28
**Plans**: 2 plans

Plans:

- [x] 29-01: Add verification for plugin manifest/runtime loading, env validation, and native tool registration/contract smoke coverage
- [x] 29-02: Run end-to-end read/write verification through the native plugin path and capture any migration notes needed from the previous OpenClaw setup

**Details:**
- Requirements: `VER-01`
- Outcomes: release-ready evidence that the native plugin path works and that OpenClaw can stop depending on an MCP bridge for MikoshiTracker.
- Success criteria:
  1. Verification proves the plugin loads, validates config, and exposes the expected tool inventory.
  2. At least one read flow and one safe mutation flow succeed against the real MikoshiTracker API through the native plugin runtime.
  3. The migration from the older OpenClaw bridge path is documented well enough that operators can switch without guesswork.

---

## Milestone Summary

**Decimal Phases:**

- None

**Key Decisions:**

- Decision: OpenClaw should expose MikoshiTracker through a native plugin instead of another MCP bridge. Outcome: ✓ Good
- Decision: The plugin should reuse the shipped API client/contracts/auth seam instead of forking host-specific domain logic. Outcome: ✓ Good
- Decision: Native success and failure payloads should be stable structured envelopes designed for agents. Outcome: ✓ Good
- Decision: OpenClaw docs and verification should become native-first while MCP remains the generic-host path. Outcome: ✓ Good
- Decision: Repository proof should include a real API-backed native read/write integration test before milestone close. Outcome: ✓ Good

**Issues Resolved:**

- OpenClaw no longer depends on the `skill -> mcporter -> MCP -> API` chain to reach MikoshiTracker tools.
- The native plugin now exposes the full habits/today/stats surface while reusing shared API/auth/runtime primitives.
- Agents now receive stable machine-readable success envelopes and clearer `network`, `timeout`, `not_found`, and `wrong_kind` failure categories.
- OpenClaw docs, examples, migration notes, and verification gates now point to the native plugin path first.

**Issues Deferred:**

- Real OpenClaw UI/plugin-loader and secret-store confirmation remain external-host-only follow-up outside this repository harness.
- Remote Streamable HTTP transport and MCP Registry follow-through remain valid backlog items for the generic MCP path.
- Additional native host integrations remain deferred until a later milestone selects them explicitly.

**Technical Debt Incurred:**

- The root script names `verify:openclaw` and `verify:openclaw:full` were kept stable even though their evidence shifted from MCP-first to native-plugin-first.

---

_For current project status, see `.planning/ROADMAP.md`._
