{
  "name": "@mikoshi-tracker/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "bun --watch src/server.ts",
    "test": "vitest run",
    "vitest": "vitest",
    "dump:openapi": "bun scripts/dump-v1-openapi.ts",
    "prisma:generate": "prisma generate --config ../../prisma.config.ts --schema ../../prisma/schema.prisma",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "start": "bun src/server.ts"
  },
  "dependencies": {
    "@fastify/cors": "^11.2.0",
    "@fastify/helmet": "^13.0.2",
    "@fastify/multipart": "^10.0.0",
    "@fastify/rate-limit": "^10.3.0",
    "@mikoshi-tracker/contracts": "workspace:*",
    "@prisma/adapter-libsql": "^7.8.0",
    "@prisma/client": "^7.8.0",
    "better-auth": "^1.6.11",
    "fastify": "^5.8.5",
    "file-type": "^22.0.1",
    "sharp": "^0.34.5",
    "zod": "^4.4.3"
  }
}
