import { SettingsIndexPage } from "../../components/settings/settings-index-page";

/** Port of app/(app)/settings/page.tsx. */
export default function SettingsRoute() {
  return <SettingsIndexPage />;
}
