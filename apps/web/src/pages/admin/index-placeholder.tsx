import { StatePanel } from "../../../components/ui";

/** Temporary index while the admin views are ported (next commits). */
export default function AdminIndexPlaceholder() {
  return <StatePanel title="Admin console" description="Views are being ported here." compact />;
}
