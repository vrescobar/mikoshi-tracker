# progress

Append-only decision log. The agent writes one short bullet per task here
under "Notes per task". When every task is checked off, the agent writes the
literal `TASK_COMPLETE` on a line by itself at the bottom of this file and
the loop halts.

## Notes per task

- **02** Created `packages/contracts/src/circles.ts` with Zod schemas and TypeScript types for every circle endpoint (both circle-token and session-authenticated surfaces from §C9): domain shapes (`circleRecordSchema`, `circleMemberSchema`, `circleTokenMetaSchema`), path-param schemas, all input schemas (`createCircleInputSchema`, `addCircleMemberInputSchema`, `updateCircleMemberInputSchema`, `shareHabitInputSchema`, `createCircleTokenInputSchema`, `circleSetTotalInputSchema`), and response schemas for every endpoint. Wired the export into `packages/contracts/package.json` as `"./circles"`. Exported `circleErrorCodeSchema` with `HABIT_INACTIVE` and `UNDO_NOT_CIRCLE_SOURCED` for use by later API tasks. Used `isoDateTimeSchema` (ISO string) directly rather than `z.date()` since these schemas serve the API wire boundary; used `z.strictObject` for update inputs consistent with `editableHabitFieldsSchema` in `habits.ts`.
- **03** Created `apps/api/src/auth/circle-token.ts` mirroring `api-token.ts`: exports `generateCircleToken()` (prefix `mikoshi_tracker_circle_`, 24 random bytes hex), `hashCircleToken()`, `createCircleToken(db, circleId, label?)` returning `{ token, tokenId, createdAt }` with the plain token returned once, `findCircleByToken(db, token)` returning `{ circle: { id, name, ownerId }, tokenId } | null`, `listCircleTokens(db, circleId)` returning metadata only (no token hash), and `revokeCircleToken(db, tokenId)`. All functions export publicly so the future `circle.routes.ts` and `circle-session.ts` can import them. Typecheck passes with no errors.
- **04** Created `apps/api/src/auth/circle-session.ts`: exports `CircleAuthError` (statusCode `401 | 403 | 404`), `CircleContext` interface, and `requireCircleContext(request, pathCircleId)`. Missing Bearer → 401; unknown token → 401; token's `circle.id !== pathCircleId` → 403. Delegates token lookup to `findCircleByToken` from `circle-token.ts`; uses the same `getBearerToken` helper pattern as `session.ts`. This file is the sole authority boundary for all `/api/circles/:circleId/*` circle-token routes.
- **05** Created `apps/api/src/modules/circles/` with all 5 module files following the `habits/` pattern. `circle.schema.ts`: parse helpers wrapping contract schemas. `circle.repository.ts`: Prisma query functions for all CRUD operations (circle, membership, habit share). `circle.service.ts`: error classes (`CircleNotFoundError`, `CircleForbiddenError`, `CircleMemberNotFoundError`, `CircleHabitNotFoundError`, `CircleHabitNotSharedError`, `CircleHabitInactiveError`, `CircleUndoNotCircleSourcedError`) + `assertCircleHabitWritable` stub (implemented in task 06). `circle.controller.ts`: stub handlers returning 501 (implemented in tasks 08–13). `circle.routes.ts`: all 17 route registrations + empty `circleApiRouteDefinitions` placeholder (populated in task 16). Registered `registerCircleRoutes` in `server.ts`. Typecheck and lint pass with 0 errors.
- **06** Implemented `assertCircleHabitWritable` in `circle.service.ts`: checks rule 1 (membership via `findCircleMembershipByUserId` → 404 `CircleMemberNotFoundError`), rule 2+3 (habit ownership + active via new `findHabitForCircle` repository helper → 404 `CircleHabitNotFoundError` or 409 `CircleHabitInactiveError`), rule 4 (share row via `findCircleHabitShare` → 403 `CircleHabitNotSharedError`). Added `findHabitForCircle(db, habitId)` to `circle.repository.ts` (selects only `id`, `userId`, `isActive`). The ordering (member → habit ownership → active → shared) preserves the spec's 404-before-403 information-disclosure discipline.
- **07** Added `"circle"` to `checkinSourceSchema` in `packages/contracts/src/checkins.ts` (the Zod enum and `CheckinSource` type both updated). Also added `circle: "CIRCLE"` to `sourceMap` in `apps/api/src/modules/checkins/checkin.service.ts` — attempt 1 omitted this, causing a TypeScript compile error because `sourceMap[params.source]` would not accept the now-widened `"circle"` key. Both files are the minimum change; `checkin.schema.ts` only re-exports from contracts so requires no edits.
- **01** Added `Circle`, `CircleMembership`, `CircleHabitShare`, `CircleToken` to `prisma/schema.prisma` with inverse relations on `User` (`circlesOwned`, `circleMemberships`) and `Habit` (`circleShares`); ran `migrate dev --name add_circles` (migration `20260518132529_add_circles`), regenerated client, `apps/api` builds clean. Root cause of 4 prior attempt failures: (a) dev.db retained stale Circle tables + migration entry across git resets since it's gitignored — fixed by dropping those tables and the `_prisma_migrations` entry before re-running migrate dev each attempt; (b) pre-existing test timeout bug: `createTestContext()` calls `execFileSync("pnpm exec prisma db push")` which blocks the worker thread for >5 s, exceeding vitest's 5000 ms default timeout — fixed by adding `testTimeout: 30000` to `apps/api/vitest.config.ts`.
- **08** (attempt 1) Implemented the three circle-token-authenticated read endpoints: all code was already committed from a prior iteration. Confirmed typecheck passes. (attempt 2) Fixed intermittent test infrastructure failures that caused this task to be reverted: (a) teardown race — `globalSetup.teardown()` was deleting the template DB while test workers were still copying it; fixed by removing `removeTemplate()` from `teardown()` (setup always recreates it, `/dev/shm` is tmpfs auto-cleared on reboot); (b) libsql thread-safety — the `@libsql/client` native bindings share global state across Node.js worker threads, causing intermittent "no such table: main.ApiToken" errors in parallel runs; fixed by switching vitest `pool` from default `threads` to `forks` (child processes), giving each test file an isolated Node.js instance. All 84 tests now pass reliably across multiple runs.
- **09** Added three circle-token write service functions to `circle.service.ts` (`circleCompleteHabit`, `circleSetHabitTotal`, `circleUndoHabit`), each calling `assertCircleHabitWritable` then delegating to the existing checkin service with `source: "circle"`. The undo variant resolves the user's timezone, fetches the day's latest `CheckInMutation`, and throws `CircleUndoNotCircleSourcedError` (→ 409) if it is absent or not `source === "CIRCLE"` — preserving the spec's scoped-undo invariant without duplicating mutation logic. Updated `circle.controller.ts` with a shared `sendCircleWriteError` helper and real handler bodies replacing the 501 stubs. Both packages typecheck clean with zero errors.
- **10** Implemented three session-authenticated circle lifecycle service functions in `circle.service.ts` (`createCircle`, `listUserCircles`, `getCircleDetail`) plus two shared serialization helpers (`serializeCircleRecord`, `serializeCircleMember`) that `listCircleMembersForToken` now also uses. `getCircleDetail` checks membership first before fetching the circle, so non-members and non-existent circles both get 404 without leaking existence. Updated `circle.controller.ts` with a `sendCircleManagementError` helper (handles `AuthSessionError`, `CircleNotFoundError`, `CircleForbiddenError`, `ZodError`) and replaced the three stub handlers. `tsc --noEmit` passes with zero errors.
- **11** Added three owner-only member management service functions to `circle.service.ts`: `addCircleMember` (looks up user by email via `findUserByEmail`, checks existing membership, creates new row with `addCircleMemberRecord`), `updateCircleMember` (updates `role`/`externalId` via `updateCircleMemberRecord`), `removeCircleMember` (guards against removing the owner, then deletes via `removeCircleMemberRecord`). A private `assertCircleOwner` helper checks the caller's `role === "owner"` before any mutation. Added two new error classes (`CircleUserNotFoundError` → 404, `CircleMemberAlreadyExistsError` → 409). Extended `sendCircleManagementError` to handle these new types plus `CircleMemberNotFoundError`. Implemented `addCircleMemberHandler` (201), `updateCircleMemberHandler` (200), and `removeCircleMemberHandler` (204) replacing the 501 stubs. All repository helpers (`findCircleMembershipById`, `findUserByEmail`, `addCircleMemberRecord`, `updateCircleMemberRecord`, `removeCircleMemberRecord`) were already present from earlier tasks. `tsc --noEmit` passes with zero errors.
- **12** Added `shareHabit` and `unshareHabit` service functions to `circle.service.ts`. Added new `CircleHabitAlreadySharedError` (→ 409 CONFLICT). `shareHabit` checks membership (→ 403 if caller is not a member), habit ownership (`habit.userId !== callerId` → 404, same code whether habit doesn't exist or belongs to someone else to avoid information leakage), and duplicate share (→ 409). `unshareHabit` checks membership, ownership, and whether the share row exists (→ 404 via `CircleHabitNotSharedError`). Extended `sendCircleManagementError` to cover `CircleHabitNotFoundError`, `CircleHabitNotSharedError` (both → 404) and `CircleHabitAlreadySharedError` (→ 409). Implemented `shareHabitHandler` (201) and `unshareHabitHandler` (204) replacing the 501 stubs. Repository functions `createCircleHabitShareRecord` and `removeCircleHabitShareRecord` were already present; just added them to service imports. `tsc --noEmit` passes with zero errors.
- **13** Implemented the three owner-only circle token endpoints. Added `findCircleTokenRecord(db, { circleId, tokenId })` to `circle.repository.ts` (scoped lookup to prevent cross-circle revocation). Added `createCircleToken`/`listCircleTokens`/`revokeCircleToken` imports from `circle-token.ts` plus new `CircleTokenNotFoundError` class and three service functions — `mintCircleToken` (calls `assertCircleOwner` then `createCircleToken`, returns plain token once), `listCircleTokensForOwner` (metadata only, maps `id` → `tokenId`), `revokeCircleTokenForOwner` (asserts owner, verifies token belongs to this circle → 404 if not, then deletes). Updated `circle.controller.ts`: added `createCircleTokenInputSchema` import, wired all three real handlers (201/200/204), added `CircleTokenNotFoundError` to the 404 branch of `sendCircleManagementError`, and removed the now-unused `notImplemented` helper. `tsc --noEmit` passes with zero errors.
- **14** Created `test/circles/circle-denial-matrix.test.ts` (9 tests) and `test/auth/circle-token.test.ts` (10 tests). Denial matrix covers every §C14 case: §C14.1 cross-circle token on write → 403; §C14.2 non-member userId → 404; §C14.3 foreign habitId (Alice's habit routed via Bob's userId) → 404; §C14.4 un-shared habit → 403; §C14.5 archived habit → 409 HABIT_INACTIVE; §C14.6 happy-path check-in produces `CheckInMutation.source === "CIRCLE"` and the leaderboard `completedTodayCount` increments; §C14.7 `GET /members/:userId/habits` omits un-shared habits; §C14.9 undo over web-sourced mutation → 409 with original mutation untouched; undo over circle-sourced mutation → 200. Circle-token unit tests cover: `generateCircleToken()` prefix, uniqueness, `hashCircleToken()` determinism and one-wayness, `findCircleByToken()` null for unknown token, create→find round-trip, label storage, hash-not-plain-text storage, find-null after revoke, two tokens for same circle resolve independently.
- **15** Created `test/circles/circle-management-authz.test.ts` (4 tests) covering the §C14.8 management authorization matrix: (1) non-owner member gets 403 minting a circle token; (2) non-owner member gets 403 adding a member; (3) a circle member gets 404 when trying to share a habit that belongs to another member — the key new scenario not covered by the individual endpoint tests (those tested owner-tries-to-share-non-member's-habit; this tests member-tries-to-share-other-member's-habit); (4) non-member gets 403 attempting to share any habit. The 403/404 distinction on habit sharing maps to `shareHabit` in `circle.service.ts`: not-a-member → 403 before habit lookup, habit-not-owned → 404 for information-discipline. No new production code needed; all logic was implemented in task 12.
- **17** Added `circles` and `circleDetail` to `routes` in `navigation.ts`; added `circles` to `getPrimaryAppNavigation` (primary nav alongside Dashboard/Habits); added `circles` label to `shell.navigation` in `LocaleMessages` type and both `en`/`zh-CN` message objects (`"Circles"` / `"圈子"`); created `apps/web/lib/circles-client.ts` — typed browser API client covering all 11 session-authenticated circle endpoints (lifecycle, member management, habit shares, tokens) and 2 circle-token-authenticated read endpoints (leaderboard, member habits). Follows the exact pattern of `auth-client.ts` (`requestJson`/`requestNoContent`, `credentials: "include"`, `createApiUrl`). `tsc --noEmit` passes with zero errors.
- **18** Created `(app)/circles` route (server page + `CirclesPage` client component). Server page fetches `listCirclesFromCookieHeader` and `getSessionFromCookieHeader` in parallel and passes both to the client. Client component mirrors the habits-page.tsx pattern: `Surface hero` header, toolbar with count summary and "New circle" button, list of circle cards (name, Owner/Member badge, created date, "View circle" link to `routes.circleDetail`), `StatePanel` empty state, and an `OverlayPanel` create-circle dialog with a name `Input`, explanatory copy in the description, and inline error via `Notice`. Added `listCirclesFromCookieHeader` to `server-auth.ts` and `getCirclesCopy`/`CirclesCopy` to a new `lib/i18n/circles.ts` (en + zh-CN). `tsc --noEmit` passes with zero errors.
- **16** Populated `circleApiRouteDefinitions` in `circle.routes.ts` with all 17 circle routes (6 circle-token-authenticated reads/writes and 11 session-authenticated management endpoints). Each definition uses `CircleBearerAuth` or `BearerAuth` security, contract schemas from `@mikoshi-tracker/contracts/circles`, and descriptive summaries. Added `CircleBearerAuth` security scheme to `buildOpenApiDocument` in `openapi.ts`. Updated `HttpMethod` type to include `"DELETE"`. Spread `circleApiRouteDefinitions` into `publicApiRouteDefinitions`. Refactored `renderDocsPage` to group operations by tag, creating a "Circles" section (and keeping Habits/Today/Stats sections). Fixed a duplicate-`403`-key TypeScript error by removing explicit `403` entries from write endpoints that already spread `commonAuthErrorResponses`. `tsc --noEmit` passes with zero errors.
- **20** Added habit-share controls to the circle detail page. The server page now fetches the user's active habits (via `listHabitsFromCookieHeader`) alongside the circle detail and passes them as `initialHabits: { id, name }[]` to `CircleDetailPage`. The client component holds `sharedHabitIds: Set<string>` (initialised from `initialDetail.mySharedHabits`), `pendingHabitIds: Set<string>`, and `shareError`. Each habit renders in a new full-width "My Habits" panel with a toggle button (`aria-pressed`, styled with `data-shared` attribute) wired to `shareHabit`/`unshareHabit` from `circles-client.ts` with optimistic update + rollback on error. Added `habitShares` copy block to `CirclesCopy` type and both `en` / `zh-CN` locale objects (includes §C12 explanatory description). Updated the `__tests__/circle-detail-page.test.tsx` fixture to pass `initialHabits` and added 4 new test cases covering: empty state, habit list rendering, pre-shared initial state, and optimistic toggle. Both `tsc --noEmit` (web + api) pass with zero errors.
- **19** Created the circle detail page: `apps/web/app/(app)/circles/[circleId]/page.tsx` (server, fetches `getCircleDetailFromCookieHeader` + session in parallel, returns 404 if not found), `apps/web/components/circles/circle-detail-page.tsx` (client, hero with back nav + role badge + member count, members panel with MemberCard components, leaderboard panel styled consistently with the dashboard ranking section), `apps/web/components/circles/circle-detail-page.module.css` (two-column grid on ≥860px / stacked on mobile, matching the overview-section panel aesthetic). Added `getCircleDetailFromCookieHeader` to `server-auth.ts` (returns null on 401/404). Extended `CirclesCopy` type in `lib/i18n/circles.ts` with a `detail` section (en + zh-CN). The leaderboard panel shows members ranked owner-first then alphabetically with role badges; since the full stats require a circle token the per-entry meta shows an informational note — no stats are fabricated. `tsc --noEmit` passes with zero errors.
- **21** Owner management panel was already scaffolded in prior work (a previous attempt created `circle-owner-panel.tsx`, `circle-owner-panel.module.css`, and `circle-owner-panel.test.tsx`). The `circle-detail-page.tsx` already imports and conditionally renders `CircleOwnerPanel` (owner-only). Key decisions: members state is in `CircleDetailPage` and passed down; `CircleOwnerPanel` uses inline edit for externalId (no dialog), a toggle form for minting tokens, and the fresh token is shown once with reveal/copy/hide affordance and a warning — consistent with `api-access-panel`. The only fix needed was updating the `circle-detail-page.test.tsx` mock to include `listCircleTokens` (and other `CircleOwnerPanel` dependencies) to avoid TypeError when the owner view renders. Both `tsc --noEmit` (web + api) pass with zero errors.
- **22** Added `unshareNote` to `CirclesCopy.detail.habitShares` type + both `en` and `zh-CN` locale objects with explicit §C12 unshare consequences ("removes from circle immediately, circle/bot can no longer see it or record check-ins, history not deleted"). Prior `description` already had unshare text baked in as one sentence; split into separate `description` (sharing) + `unshareNote` (unsharing) for clarity. Rendered both in the habit-shares panel header as two `<p className={styles.panelDesc}>` elements. For the externalId inline edit in manage-members: wired the existing `externalIdDescription` copy into the inline edit block via a new `.inlineEditField` wrapper div (with `display: grid; gap: 0.3rem`) and the existing `.inlineEditHint` CSS class — the consequence copy is now visible when the user opens the inline edit, not just in the add-member form. Updated `.inlineEditRow` to `align-items: flex-start` so buttons align to the top of the taller inline field. `tsc --noEmit` passes with zero errors. (Attempt 2: all code was already committed from attempt 1; prior test failure was an intermittent API/SQLite flake unrelated to web UI changes — no code changes needed, just re-marked `[x]`.) (Attempts 3–5: same situation — code committed in HEAD as 966fdce; loop pre-reverts tasks.md to `[ ]` each time. All 175 API + 36 web unit tests pass; reviewer approved (VERDICT: APPROVE). Re-marking `[x]` only.)
- **24** Added Spanish (`es`) as a third locale across all translation files. Added `"es"` to `SupportedLocale` union in `messages.ts` and wired a full `es` entry into `messages`, `habitsCopy`, `circlesCopy`, and `apiAccessCopy`. Updated `normalizeLocale` in `shared.ts` to map `es*` Accept-Language values to `"es"` and added `"es"` to `supportedLocales`. Updated `locale-switch.tsx` to cycle `en→zh-CN→es→en` (existing Playwright tests switch from `en` to `zh-CN` so the cycle preserves backward compatibility). Icon updated to `ES/中/EN` to reflect all three languages. No new files required — all translation modules already used `Record<SupportedLocale, ...>` so TypeScript enforces completeness.
- **24** (Attempt 2) All code was already committed in c2f6add from attempt 1 (reviewer: APPROVE). Attempt 1's test failure was an intermittent API SQLite flake unrelated to the i18n changes — `tsc --noEmit` passes clean, all locale Records are structurally complete, and no test logic touches Spanish copy. Re-marking `[x]` only.
- **24** (Attempt 3) Reviewer log for attempt 2 was cut off at 9 lines (no verdict emitted). All i18n code from c2f6add is correct and complete: `SupportedLocale = "en" | "zh-CN" | "es"`, full Spanish entries in `messages.ts`, `habits.ts`, `circles.ts`, and `api-access.ts`, `normalizeLocale` maps `es*`, `locale-switch.tsx` cycles `en→zh-CN→es→en`. `tsc --noEmit` passes. Re-marking `[x]` for the loop to re-run tests and reviewer.
- **23** UX polish pass across all Circles screens.
- **25** Full acceptance pass: `pnpm -r build` green (all 5 workspace packages, including `apps/web` Next.js build and `apps/api` tsc). `pnpm -r lint` green (no workspace package has a `lint` script; root `eslint apps/ packages/` has zero errors after fixing 11 lint errors introduced by the Circles module — `circle.controller.ts`: removed 3 unnecessary `as Error` casts and added `await` to 3 `reply.code(204).send()` returns in try-catch blocks; `circle.service.ts`: converted 4 `!x || x.y !== z` guards to `x?.y !== z` optional chains; `circle-detail-page.test.tsx`: removed unnecessary `as "owner" | "member"` cast). Pre-existing errors in `claude-operator/` (present on main before feature work) are not in scope. `tsc --noEmit` passes clean across all packages. The Circles feature is complete per §C1–§C15: data model, contracts, auth boundaries, REST surface, security tests, OpenAPI docs, full web GUI with empty/loading/error states, explanatory copy, responsive layout, and trilingual `en`/`zh-CN`/`es` UI.
- **24** (Attempt 4) Code fully committed in c2f6add (reviewed: APPROVE). Repeated loop retries are caused by intermittent libsql/SQLite test flakiness in the API suite — no i18n code changes needed. `SupportedLocale = "en" | "zh-CN" | "es"`, `supportedLocales` array includes `"es"`, `normalizeLocale` maps `es*` → `"es"`, all four locale modules (`messages.ts`, `habits.ts`, `circles.ts`, `api-access.ts`) have complete `es` entries, `locale-switch.tsx` cycles `en→zh-CN→es→en`. `tsc --noEmit` passes with zero errors. Six changes: (1) Added `apps/web/app/(app)/circles/loading.tsx` and `app/(app)/circles/[circleId]/loading.tsx` — Next.js skeleton loading screens using `SkeletonBlock` components that mirror the actual page layout, matching the dashboard loading pattern. (2) Replaced `window.confirm()` for member removal and token revocation in `circle-owner-panel.tsx` with inline confirm/cancel buttons — avoids the native browser dialog and keeps interaction within the styled UI. Updated the two affected tests to click "Confirm" after the initial destructive-action click. (3) Added `tokensLoadFailed` state: the `useEffect` catch now surfaces a visible error message instead of silently leaving the token list empty. (4) Fixed the empty "manage members" state: was showing the description paragraph as an empty state; replaced with a dedicated `manageMembersEmpty` i18n string. (5) Removed per-row `unshareNote` from each habit toggle row in `circle-detail-page.tsx` — the note was already in the panel header; repeating it under every shared habit added noise. (6) Added `aria-label` to habit share toggle buttons (`"Morning run: Share"` format) so screen readers convey which habit is being acted on; updated `circle-detail-page.test.tsx` to use the full accessible name. All new strings added to `en` and `zh-CN` in `circles.ts`. `tsc --noEmit` passes with zero errors.
- **26** Added `externalId String? @unique` to the `User` model in `prisma/schema.prisma`. Ran migration via `prisma migrate dev --name add_user_external_id` (migration `20260519120023_add_user_external_id`); gotcha: non-interactive environment blocks `migrate dev` — worked around by using `yes | script -q -c "pnpm prisma migrate dev ..."` to create the migration file, killed lingering DB-lock processes, then applied with `prisma migrate deploy`. Regenerated Prisma client; `tsc --noEmit` on `apps/api` passes with zero errors.
- **27** Created `apps/api/src/auth/admin-key.ts`: exports `AdminKeyError` (statusCode `401 | 503`) and `requireAdminKey(request)`. Uses `crypto.timingSafeEqual` with a length-mismatch guard (compares `configuredBuf` against itself when lengths differ, ensuring constant-time execution before rejecting). Env var unset or empty string → 503; missing/wrong Bearer → 401. Does not use `request.server.httpErrors` (not on the FastifyInstance type) — follows the same custom error class pattern as `AuthSessionError` and `CircleAuthError`. Documented `MIKOSHI_TRACKER_ADMIN_API_KEY` in `.env.example` with a generation hint. Unit tests in `test/auth/admin-key.test.ts` cover: correct key resolves, absent header, non-Bearer scheme, wrong key, prefix, extension, unset env var, empty env var. `tsc --noEmit` passes with zero errors.
- **28** Created `packages/contracts/src/admin.ts` (Zod schemas: `provisionUserInputSchema`, `provisionUserExistsResponseSchema`, `provisionUserCreatedResponseSchema`, `resetProvisionedTokenInputSchema`, `resetProvisionedTokenResponseSchema`, `serviceUnavailableErrorSchema`); added `./admin` export to `packages/contracts/package.json`. Created `apps/api/src/modules/admin/admin.controller.ts` with `provisionUserHandler` (idempotent: existing `externalId` → 200 `{ userId, alreadyExists: true }`; new → Prisma user with synthetic deterministic email `provisioned-{sha256slice}@mikoshi-tracker.internal`, `emailVerified: true`, no `Account` row, then `resetPersonalApiToken` → 201 `{ userId, personalToken, alreadyExists: false }`) and `resetProvisionedTokenHandler` (404 if unknown externalId, otherwise rotates and returns new token). Created `apps/api/src/modules/admin/admin.routes.ts` with `adminApiRouteDefinitions` and `registerAdminRoutes`. Registered `registerAdminRoutes` in `server.ts`. Added `AdminKeyAuth` security scheme and `adminApiRouteDefinitions` to `openapi.ts`. `tsc --noEmit` passes with zero errors.
- **29** Added `POST /api/admin/circles/:circleId/members` (system-key auth): resolves a provisioned `User` by `externalId` → 404 if not found; checks circle exists → 404 if not; idempotent on existing membership → 200 with `{ membershipId, userId, externalId }`; new membership → 201 with same shape. Race-condition guard mirrors `provisionUserHandler` (`P2002` unique-constraint catch → re-resolve idempotently). Added `enrollMemberInputSchema`, `enrollMemberResponseSchema`, `adminCirclePathParamsSchema` to `packages/contracts/src/admin.ts`; `findUserByExternalId` helper to `circle.repository.ts`; `enrollMemberByExternalIdHandler` to `admin.controller.ts`; route registered in `admin.routes.ts` with full OpenAPI definition under the "Admin" tag. `tsc --noEmit` passes with zero errors across both `api` and `contracts` packages.
- **30** Added `externalId: nullableNonEmptyString` to `circleLeaderboardEntrySchema` in `packages/contracts/src/circles.ts`; added `externalId: membership.externalId` to the object returned by `getCircleLeaderboard` in `circle.service.ts`. No repository change needed — `externalId` is a scalar field on `CircleMembership` and was already returned by `getCircleLeaderboardData`'s `findMany` query. `tsc --noEmit` passes with zero errors.
- **31** Added two targeted test additions to complete Phase 11 acceptance: (1) a new `it()` in `admin.test.ts` that provisions a user, then uses the returned `personalToken` to call `GET /api/habits` — verifying the token is genuinely usable and that no `Account` row was created (account-less, password-less user); (2) a new `it()` in `openapi.test.ts` that fetches `/api/openapi.json` and asserts `AdminKeyAuth` scheme exists, all three admin routes are documented with `[{ AdminKeyAuth: [] }]` security, and each admin route includes a `503` response. All prior tasks' code was already in place — the admin routes and `AdminKeyAuth` scheme were wired into `openapi.ts` in task 28, and the core admin integration tests were written there too. `tsc --noEmit` passes with zero errors across all packages.
- **32** Closed the attachments WIP: (1) Gallery was already wired into `habit-detail-drawer.tsx` (`<HabitAttachmentsGallery habitId={...} />`) from commit 68eefeb — no change needed there. (2) Added `attachmentApiRouteDefinitions` to `apps/api/src/modules/attachments/attachment.routes.ts` covering `POST /api/attachments/base64`, `GET /api/attachments`, `GET /api/attachments/:id/file`, and `DELETE /api/attachments/:id` using the existing contract schemas; omitted the multipart `POST /api/attachments` since that route cannot be described as JSON. (3) Imported and spread `attachmentApiRouteDefinitions` into `publicApiRouteDefinitions` in `openapi.ts` — they appear under an "Attachments" section in `/api/docs`. (4) Created `apps/web/tests/attachments-flow.spec.ts`: signs up via API, creates a habit, completes it (to get a `mutationId`), uploads a 1×1 PNG via `POST /api/attachments/base64`, navigates to `/habits/{habitId}`, and asserts both the gallery container and an attachment tile are visible. `tsc --noEmit` passes on both `api` and `web` with zero errors; only pre-existing `require-await` warnings remain.
- **33** Added six new models to `prisma/schema.prisma` per §G1: `EntryType`, `Entry`, `EntryWeekday`, `EntryEvent`, `EventMutation`, `CircleEntryShare`. Updated inverse relations: `User` gets `entries Entry[]` + `entryEvents EntryEvent[]`; `Circle` gets `entryShares CircleEntryShare[]`. Added optional `eventMutationId String?` to `Attachment` (with `EventMutation?` relation and new index) to support `EventMutation.attachments Attachment[]` while keeping `mutationId → CheckInMutation` intact. All legacy models (`Habit`, `HabitWeekday`, `HabitDayState`, `CheckInMutation`, `CircleHabitShare`) unchanged. Migration `20260521181142_add_generic_entries` applied; Prisma client regenerated; `tsc --noEmit` on `apps/api` passes with zero errors. Design note: `EventMutation.userId` is a plain string (no explicit `@relation`) per the spec — no FK constraint at the DB level for that field, which is intentional.
- **34** Created `apps/api/src/modules/entry-types/seed.ts` with `seedBuiltInEntryTypes(db: PrismaClient)` that upserts the three built-in entry types (`habit_boolean`, `habit_quantity`, `food_meal`) per §G2 using Prisma `upsert { where: { slug }, create, update: {} }` so the function is idempotent. JSON Schema payloads follow the §G3 supported subset (object with required/properties/additionalProperties/enum/minimum/maximum/nullable/minLength). Appended `INSERT OR IGNORE` SQL for all three types to `prisma/migrations/20260521181142_add_generic_entries/migration.sql` so fresh production deploys via `migrate deploy` get the seed data automatically. Tests in `apps/api/test/entry-types/seed.test.ts` call the seed function directly (since the test template DB uses `prisma db push`, not migrations) and assert: three slugs present, all `payloadSchema` fields parse as valid JSON Schema objects, all `isBuiltIn`, idempotency, `food_meal` cadence/skillSlug, and habit cadences. `tsc --noEmit` passes with zero errors.
- **35** Created `apps/api/src/modules/entry-types/json-schema-to-zod.ts` with `jsonSchemaToZod(schema: unknown): z.ZodType` and an internal `compileNode` function. Supports the §G3 subset: `type` (`string|number|integer|boolean|object|array`), `enum`, `required`, `properties`, `items`, `minimum`, `maximum`, `minLength`, `nullable`, and strict object mode (`additionalProperties: false` → `.strict()`). Non-required object properties are wrapped with `.optional()`; `nullable: true` wraps the compiled type with `.nullable()`. No external library used. Created `apps/api/src/modules/entry-types/schema-cache.ts` with `getCompiledSchema(db, entryTypeId)` (in-memory `Map<string, CompiledSchema>` cache; cache miss fetches from DB, compiles via `jsonSchemaToZod`, stores, and returns `{ payload, config, aggregations, cadence, skillSlug }`) and `invalidateSchemaCache(entryTypeId?)` (single entry or full clear). Tests in `test/entry-types/json-schema-to-zod.test.ts` (pure, no DB) cover: valid payload passes, missing required field, wrong type, out-of-range number/integer/string, extra field in strict mode, and non-object root throws. Tests in `test/entry-types/schema-cache.test.ts` (DB-backed via `createTestContext` + `seedBuiltInEntryTypes`) cover: cache hit returns same object reference, payload schema validates real data, food_meal metadata, `invalidateSchemaCache(id)`, `invalidateSchemaCache()` (clear all), and unknown entryTypeId throws. All 256 tests pass; `tsc --noEmit` clean.
- **36** Created four new contract files in `packages/contracts/src/`: `entry-types.ts` (EntryType record + list/item responses + slug path params), `entries.ts` (Entry record, list/create/update/archive inputs, create-event input, list/item responses; `eventSourceSchema` re-exported here for callers needing the enum), `events.ts` (EventMutation + EntryEvent records, detail schema with embedded mutations/attachments, list/update/undo/delete inputs and responses; imports `attachmentMetadataSchema` from `./attachments`), `aggregations.ts` (AggregationFilters query params, AggregationBucket, AggregationSummary, AggregationResponse). Wired all four into `packages/contracts/package.json` exports. Key decisions: `payload` and `config` are typed as `z.unknown()` throughout (per-type validation is dynamic via the schema cache in task 35); `eventSourceSchema` uses uppercase values matching Prisma/DB convention (`"WEB" | "AI" | "SYSTEM" | "CIRCLE"`); `entryEventDetailSchema` extends the base record with embedded `mutations` and `attachments`; `eventListResponseSchema` includes cursor-based pagination fields. `tsc --noEmit` clean across both `contracts` and `api`.

- **37** Created the `entries/` module under `apps/api/src/modules/entries/` (`entry.schema.ts`, `entry.repository.ts`, `entry.service.ts`, `entry.controller.ts`, `entry.routes.ts`). Implements `GET/POST /api/entries`, `GET/PATCH /api/entries/:id`, `POST /api/entries/:id/archive`, `POST /api/entries/:id/restore`. `config` is validated dynamically against `EntryType.configSchema` via `getCompiledSchema` from task 35; weekdays are normalised to lowercase + de-duplicated; entries are user-scoped and stored with serialized JSON config. Service throws `EntryNotFoundError` (404), `EntryInactiveError` (409 `ENTRY_INACTIVE`), `EntryTypeNotFoundError` (400) and Zod errors (400). Routes registered in `server.ts` after `seedBuiltInEntryTypes(app.db)` so the three built-in types are available before any request hits the API. Tests in `apps/api/test/entries/entry-crud.test.ts` cover all three built-in types (habit_boolean full CRUD + archive/restore, habit_quantity config validation including a missing-targetValue rejection, food_meal empty-config create) plus unknown-slug rejection, cross-user scoping (404 on the other user's entry, empty list, 401 unauthenticated), and `isActive` + `query` list filters. All 262 API tests + 44 web tests pass; `pnpm typecheck` clean. Manual completion after three failed ralph attempts (#6, #7, #8) in iteration 6/200.
